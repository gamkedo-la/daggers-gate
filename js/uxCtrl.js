/**
 * Abstract class that provides control context for game states
 * Subclasses under this should override methods to provide necessary functionality.
 */
class UxCtrl {
    // CONSTRUCTOR ---------------------------------------------------------
    constructor(spec={}) {
    }

    // METHODS -------------------------------------------------------------
    update(ctx) {
    }

    keyPressed(key) {
    }

    keyReleased(key) {
    }

    mouseClicked(x, y) {
    }

    mouseMoved(x, y) {
    }

}